# OmniDesk v2.3.0 — social posts

Release: https://github.com/carloluisito/omnidesk/releases/tag/v2.3.0
Headline: remote access is now genuinely usable from a phone.

Copy the text under each heading (plain text, no markdown decoration to strip).

---

## Threads  (~417 / 500 chars)

OmniDesk v2.3.0 📱

Remote access now actually works from your phone. Open a session in the browser and you get a focused mobile layout + a real keyboard — tap to type, plus an on-screen key bar for Esc, Tab, Ctrl+C, arrows & newline (the keys a CLI needs that phones don't have).

Install it as a PWA (notch-safe, full screen) and keep your Claude Code / Codex sessions going anywhere.

→ github.com/carloluisito/omnidesk

---

## Reddit

Title:

OmniDesk v2.3.0 — you can now actually drive your Claude Code / Codex sessions from your phone

Body:

OmniDesk is a desktop app that hosts your AI coding CLIs (Claude Code, Codex) with a proper multi-repo/multi-session UI. It's had remote access for a while — it serves its own UI over a one-click Cloudflare tunnel (token-secured, binds to localhost only) so you can reach your live sessions from any browser.

The catch: on a phone it was basically read-only. xterm.js doesn't raise the mobile keyboard on its own, and there was no way to send Esc / Tab / Ctrl-C / arrows. So you could watch a session, not work in it.

v2.3.0 fixes that:

- Focused mobile layout — instead of cramming the desktop's multi-panel shell onto a 6" screen, phones get a session drawer over a full-screen terminal.
- Typable terminal — tap to raise the keyboard; the view reflows (visualViewport) as the keyboard opens so your prompt stays visible.
- On-screen key bar — Esc, Tab, sticky Ctrl (Ctrl+C etc.), arrows, newline, common symbols, paste. Agent Ctrl+C opens a "close session?" confirm instead of killing Claude.
- PWA polish — installs full-screen, respects the notch/home-indicator safe areas.

Desktop behavior is completely unchanged — the mobile mode only activates for touch clients over remote.

Repo + installers (Win/macOS/Linux): github.com/carloluisito/omnidesk

Would love feedback from anyone who runs their coding agents remotely — especially edge cases on iOS Safari vs Android Chrome.

Suggested subreddits: r/ClaudeAI, r/programming (Show-style), r/SideProject

---

## LinkedIn

🚀 Shipped OmniDesk v2.3.0 — your AI coding sessions, now genuinely usable from your phone.

OmniDesk is a desktop workspace for AI coding CLIs like Claude Code and Codex. It already let you reach your live sessions remotely through a secure one-click tunnel — but on a phone the terminal was effectively read-only. You could watch, not work.

This release changes that:

📱 A focused mobile layout — a session drawer over a full-screen terminal, instead of a shrunk-down desktop UI.
⌨️ A typable terminal — tap to bring up the keyboard, with the view reflowing as it opens so your prompt stays in view.
🎛️ An on-screen key bar for the keys a CLI needs but a touch keyboard lacks: Esc, Tab, Ctrl-combos, arrows, newline, paste.
📲 Installable as a PWA, full-screen and safe-area aware.

The desktop experience is untouched — mobile mode activates only for touch clients over remote access.

The goal: step away from your desk and keep your agents moving from wherever you are.

Repo + installers for Windows, macOS, and Linux → github.com/carloluisito/omnidesk

#AI #DeveloperTools #ClaudeCode #Electron #BuildInPublic
